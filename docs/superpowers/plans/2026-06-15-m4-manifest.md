# M4：显化日记 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现显化日记的完整流程——星空专属视觉、写意图 + 选分类、AI 显化回响（模式 2）和轻量关键词分析（模式 3）。

**Architecture:** 新增 `/manifest` 页面（星空主题），复用 M2 的 AI 调用基础设施，新增两个 API Route（`/api/ai/echo`、`/api/ai/analyze`）和一套 manifest CRUD 接口。显化页面使用独立的星空 CSS 变量集，与白天/夜晚主题并行。

**Tech Stack:** Next.js App Router, Supabase, TailwindCSS, DeepSeek API (via OpenAI SDK), Web Speech API

---

## 前置依赖

- M1 完成：Supabase 项目存在，`manifest_entries` 表已建好，RLS 已配置
- M2 完成：`lib/ai/client.ts`、`lib/ai/prompts/` 目录结构已存在
- M3 完成：主题切换基础设施 `ThemeProvider` 已存在

---

## 文件结构

```
src/
├─ app/
│  ├─ manifest/
│  │  └─ page.tsx                    # 显化日记主页面
│  └─ api/
│     ├─ manifest/
│     │  └─ route.ts                 # manifest CRUD
│     └─ ai/
│        ├─ echo/
│        │  └─ route.ts              # 模式 2：显化回响
│        └─ analyze/
│           └─ route.ts              # 模式 3：关键词 + 点睛
├─ components/
│  └─ manifest/
│     ├─ IntentionInput.tsx          # 意图输入区（文字 + 语音）
│     ├─ CategorySelector.tsx        # 六分类选择器
│     ├─ EchoBubble.tsx              # 显化回响气泡
│     ├─ KeywordTags.tsx             # 关键词标签展示
│     ├─ ManifestCard.tsx            # 单条显化记录卡片
│     └─ StarfieldBackground.tsx     # 星空粒子背景
├─ lib/
│  ├─ ai/
│  │  └─ prompts/
│  │     ├─ echo.ts                  # 模式 2 prompt
│  │     └─ analyze.ts               # 模式 3 prompt
│  └─ supabase/
│     └─ manifest.ts                 # manifest 数据操作
└─ types/
   └─ manifest.ts                    # manifest 类型定义
```

---

### Task 1：Manifest 类型定义与 Supabase 操作

**Files:**
- Create: `src/types/manifest.ts`
- Create: `src/lib/supabase/manifest.ts`

- [ ] **Step 1: 写 manifest 类型定义**

```typescript
// src/types/manifest.ts

export type ManifestCategory =
  | "self"
  | "relationship"
  | "career"
  | "health"
  | "abundance"
  | "other";

export const MANIFEST_CATEGORY_LABELS: Record<ManifestCategory, string> = {
  self: "自我",
  relationship: "关系",
  career: "事业",
  health: "身心",
  abundance: "丰盛",
  other: "其他",
};

export const MANIFEST_CATEGORY_ICONS: Record<ManifestCategory, string> = {
  self: "🌱",
  relationship: "💕",
  career: "🎯",
  health: "🌿",
  abundance: "✨",
  other: "🔮",
};

export interface ManifestEntry {
  id: string;
  userId: string;
  createdAt: string;
  entryDate: string;
  intention: string;
  category: ManifestCategory;
  aiEcho: string | null;
  keywords: string[];
}

export interface ManifestCreateInput {
  intention: string;
  category: ManifestCategory;
}

export interface EchoResponse {
  echo: string;
}

export interface AnalyzeResponse {
  keywords: string[];
  insight: string;
}
```

- [ ] **Step 2: 写 manifest Supabase 操作函数**

```typescript
// src/lib/supabase/manifest.ts

import { createClient } from "@/lib/supabase/server";
import type {
  ManifestEntry,
  ManifestCreateInput,
  ManifestCategory,
} from "@/types/manifest";

function getEntryDate(): string {
  const now = new Date();
  // 凌晨 0-4 点写的归属前一天
  if (now.getHours() < 4) {
    const yesterday = new Date(now);
    yesterday.setDate(yesterday.getDate() - 1);
    return yesterday.toISOString().split("T")[0];
  }
  return now.toISOString().split("T")[0];
}

export async function createManifestEntry(
  userId: string,
  input: ManifestCreateInput
): Promise<ManifestEntry> {
  const supabase = await createClient();
  const entryDate = getEntryDate();

  const { data, error } = await supabase
    .from("manifest_entries")
    .insert({
      user_id: userId,
      entry_date: entryDate,
      intention: input.intention,
      category: input.category,
      ai_echo: null,
      keywords: [],
    })
    .select()
    .single();

  if (error) throw error;
  return mapToManifestEntry(data);
}

export async function updateManifestEcho(
  entryId: string,
  echo: string
): Promise<void> {
  const supabase = await createClient();
  const { error } = await supabase
    .from("manifest_entries")
    .update({ ai_echo: echo })
    .eq("id", entryId);

  if (error) throw error;
}

export async function updateManifestAnalysis(
  entryId: string,
  keywords: string[],
  insight: string
): Promise<void> {
  const supabase = await createClient();
  // insight 暂存到 ai_echo 末尾，v2 有独立字段时再迁移
  const { data: current } = await supabase
    .from("manifest_entries")
    .select("ai_echo")
    .eq("id", entryId)
    .single();

  const existingEcho = current?.ai_echo ?? "";
  const { error } = await supabase
    .from("manifest_entries")
    .update({
      keywords,
      ai_echo: existingEcho
        ? `${existingEcho}\n\n💡 ${insight}`
        : `💡 ${insight}`,
    })
    .eq("id", entryId);

  if (error) throw error;
}

export async function getManifestEntriesByDate(
  userId: string,
  date: string
): Promise<ManifestEntry[]> {
  const supabase = await createClient();
  const { data, error } = await supabase
    .from("manifest_entries")
    .select()
    .eq("user_id", userId)
    .eq("entry_date", date)
    .order("created_at", { ascending: false });

  if (error) throw error;
  return (data ?? []).map(mapToManifestEntry);
}

export async function getRecentManifestEntries(
  userId: string,
  limit: number = 10
): Promise<ManifestEntry[]> {
  const supabase = await createClient();
  const { data, error } = await supabase
    .from("manifest_entries")
    .select()
    .eq("user_id", userId)
    .order("created_at", { ascending: false })
    .limit(limit);

  if (error) throw error;
  return (data ?? []).map(mapToManifestEntry);
}

function mapToManifestEntry(data: Record<string, unknown>): ManifestEntry {
  return {
    id: data.id as string,
    userId: data.user_id as string,
    createdAt: data.created_at as string,
    entryDate: data.entry_date as string,
    intention: data.intention as string,
    category: data.category as ManifestCategory,
    aiEcho: data.ai_echo as string | null,
    keywords: (data.keywords as string[]) ?? [],
  };
}
```

- [ ] **Step 3: Commit**

```bash
git add src/types/manifest.ts src/lib/supabase/manifest.ts
git commit -m "feat(manifest): add types and supabase operations for manifest entries"
```

---

### Task 2：模式 2 & 模式 3 Prompt 定义

**Files:**
- Create: `src/lib/ai/prompts/echo.ts`
- Create: `src/lib/ai/prompts/analyze.ts`

- [ ] **Step 1: 写模式 2——显化回响 prompt**

```typescript
// src/lib/ai/prompts/echo.ts

export function buildEchoPrompt(intention: string): string {
  return `# 你是谁

你是用户写下意图时的"宇宙回声"。

你的本质：
- 温柔但有力量的见证者
- 你确认用户的意图"已被收到"
- 你的语气笃定，像一件已经发生的事，而不是一个愿望

你不是：
- 心理咨询师（不分析、不点评）
- 人生教练（不给建议、不设目标）
- 玄学神棍（不说"宇宙能量场"、"频率"、"磁场"这种话）
- 诗人（你有诗意但不矫情，不押韵不堆砌意象）

# 你的回应规则

✅ 必须做：
- 用"收到了"或类似的话开头，确认意图已被见证
- 从用户写下的意图里提取一个具体的"已经发生的证据"——他愿意写下这件事，就是证据
- 语气温柔、笃定、日常。用"嗯"、"真的"这种口语词
- 控制在 50 字以内

❌ 严禁：
- 分析、点评、给建议（"你可以试试..."）
- 玄学话术（"宇宙正在回应你"、"能量已发出"）
- 超过 50 字
- 反问句
- "作为一个 AI..."
- 加油、相信、坚持这类鸡汤词

# 用户的意图

${intention}

# 输出格式

直接输出纯文本，不要 JSON，不要 markdown，不要引号。就是你回给用户的那句话。`;
}
```

- [ ] **Step 2: 写模式 3——显化轻量分析 prompt**

```typescript
// src/lib/ai/prompts/analyze.ts

export function buildAnalyzePrompt(intention: string): string {
  return `# 你是谁

你是一个语义提取引擎。你从用户写的显化意图里提取关键词和一句话洞察。

# 任务

从下面的意图中提取 3-5 个关键词，并写一句不超过 30 字的点睛话。

关键词规则：
- 提取的是"核心渴望词"，不是表面词
- 例：用户写"我想找到一个真正理解我的人" → 关键词是"被理解"、"深度连接"，不是"找"、"人"
- 关键词应该是名词或动名词短语，每个 2-4 个字
- 不要提取虚词、否定词

点睛话规则：
- 不超过 30 字
- 不评价、不建议
- 是对核心渴望的精准概括，像一面镜子
- 例：用户写"我想成为一个平静有秩序的人" → "你渴望的不是秩序本身，是内心不再混乱的自由"

# 用户的意图

${intention}

# 输出格式

输出严格的 JSON，不要包在 \`\`\`json 里：
{
  "keywords": ["关键词1", "关键词2", "关键词3"],
  "insight": "一句点睛话，不超过30字"
}`;
}
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/ai/prompts/echo.ts src/lib/ai/prompts/analyze.ts
git commit -m "feat(ai): add echo and analyze prompt builders for manifest modes"
```

---

### Task 3：API Route——模式 2 显化回响 & 模式 3 分析

**Files:**
- Create: `src/app/api/ai/echo/route.ts`
- Create: `src/app/api/ai/analyze/route.ts`

- [ ] **Step 1: 写 /api/ai/echo route**

```typescript
// src/app/api/ai/echo/route.ts

import { NextRequest, NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { getAIClient } from "@/lib/ai/client";
import { buildEchoPrompt } from "@/lib/ai/prompts/echo";
import { logAiCall } from "@/lib/ai/log";

export async function POST(request: NextRequest) {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();

  if (!user) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  const { entryId, intention } = await request.json();

  if (!entryId || !intention) {
    return NextResponse.json(
      { error: "entryId and intention are required" },
      { status: 400 }
    );
  }

  const startTime = Date.now();
  const client = getAIClient();
  const prompt = buildEchoPrompt(intention);

  const response = await client.chat.completions.create({
    model: process.env.AI_MODEL ?? "DeepSeek-V4-Flash",
    messages: [{ role: "user", content: prompt }],
    max_tokens: 150,
    temperature: 0.8,
    stream: false,
  });

  const latencyMs = Date.now() - startTime;
  const echo = response.choices[0]?.message?.content?.trim() ?? "";
  const tokensIn = response.usage?.prompt_tokens ?? 0;
  const tokensOut = response.usage?.completion_tokens ?? 0;

  // 更新数据库
  const { error: updateError } = await supabase
    .from("manifest_entries")
    .update({ ai_echo: echo })
    .eq("id", entryId)
    .eq("user_id", user.id);

  if (updateError) {
    console.error("Failed to update manifest echo:", updateError);
  }

  // 记录 AI 调用日志
  await logAiCall({
    userId: user.id,
    mode: "manifest_echo",
    tokensIn,
    tokensOut,
    latencyMs,
  });

  return NextResponse.json({ echo });
}
```

- [ ] **Step 2: 写 /api/ai/analyze route**

```typescript
// src/app/api/ai/analyze/route.ts

import { NextRequest, NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { getAIClient } from "@/lib/ai/client";
import { buildAnalyzePrompt } from "@/lib/ai/prompts/analyze";
import { logAiCall } from "@/lib/ai/log";

export async function POST(request: NextRequest) {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();

  if (!user) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  const { entryId, intention } = await request.json();

  if (!entryId || !intention) {
    return NextResponse.json(
      { error: "entryId and intention are required" },
      { status: 400 }
    );
  }

  const startTime = Date.now();
  const client = getAIClient();
  const prompt = buildAnalyzePrompt(intention);

  const response = await client.chat.completions.create({
    model: process.env.AI_MODEL ?? "DeepSeek-V4-Flash",
    messages: [{ role: "user", content: prompt }],
    max_tokens: 200,
    temperature: 0.3,
    stream: false,
  });

  const latencyMs = Date.now() - startTime;
  const raw = response.choices[0]?.message?.content?.trim() ?? "";
  const tokensIn = response.usage?.prompt_tokens ?? 0;
  const tokensOut = response.usage?.completion_tokens ?? 0;

  let keywords: string[] = [];
  let insight = "";

  try {
    const parsed = JSON.parse(raw);
    keywords = parsed.keywords ?? [];
    insight = parsed.insight ?? "";
  } catch {
    console.error("Failed to parse analyze response:", raw);
  }

  // 更新数据库
  const { error: updateError } = await supabase
    .from("manifest_entries")
    .update({
      keywords,
      ai_echo: supabase.rpc // 用已有 ai_echo 拼接 insight
        ? undefined // 不用 rpc
        : undefined,
    })
    .eq("id", entryId)
    .eq("user_id", user.id);

  // 单独处理 insight 追加到 ai_echo
  if (keywords.length > 0 || insight) {
    const { data: current } = await supabase
      .from("manifest_entries")
      .select("ai_echo")
      .eq("id", entryId)
      .single();

    const existingEcho = current?.ai_echo ?? "";
    await supabase
      .from("manifest_entries")
      .update({
        keywords,
        ai_echo: existingEcho
          ? `${existingEcho}\n\n💡 ${insight}`
          : `💡 ${insight}`,
      })
      .eq("id", entryId);
  }

  // 记录 AI 调用日志
  await logAiCall({
    userId: user.id,
    mode: "manifest_analysis",
    tokensIn,
    tokensOut,
    latencyMs,
  });

  return NextResponse.json({ keywords, insight });
}
```

- [ ] **Step 3: Commit**

```bash
git add src/app/api/ai/echo/route.ts src/app/api/ai/analyze/route.ts
git commit -m "feat(api): add echo and analyze API routes for manifest AI modes"
```

---

### Task 4：API Route——Manifest CRUD

**Files:**
- Create: `src/app/api/manifest/route.ts`

- [ ] **Step 1: 写 manifest CRUD route**

```typescript
// src/app/api/manifest/route.ts

import { NextRequest, NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import type { ManifestCategory } from "@/types/manifest";

// GET /api/manifest?date=2026-06-15 或 /api/manifest?limit=10
export async function GET(request: NextRequest) {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();

  if (!user) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  const { searchParams } = new URL(request.url);
  const date = searchParams.get("date");
  const limit = parseInt(searchParams.get("limit") ?? "10", 10);

  let query = supabase
    .from("manifest_entries")
    .select()
    .eq("user_id", user.id);

  if (date) {
    query = query.eq("entry_date", date);
  }

  query = query
    .order("created_at", { ascending: false })
    .limit(date ? 100 : limit);

  const { data, error } = await query;

  if (error) {
    return NextResponse.json({ error: error.message }, { status: 500 });
  }

  return NextResponse.json({ entries: data });
}

// POST /api/manifest
export async function POST(request: NextRequest) {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();

  if (!user) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  const { intention, category } = await request.json();

  if (!intention || !category) {
    return NextResponse.json(
      { error: "intention and category are required" },
      { status: 400 }
    );
  }

  const validCategories: ManifestCategory[] = [
    "self",
    "relationship",
    "career",
    "health",
    "abundance",
    "other",
  ];

  if (!validCategories.includes(category)) {
    return NextResponse.json(
      { error: `category must be one of: ${validCategories.join(", ")}` },
      { status: 400 }
    );
  }

  // 凌晨 0-4 点写的归属前一天
  const now = new Date();
  let entryDate: string;
  if (now.getHours() < 4) {
    const yesterday = new Date(now);
    yesterday.setDate(yesterday.getDate() - 1);
    entryDate = yesterday.toISOString().split("T")[0];
  } else {
    entryDate = now.toISOString().split("T")[0];
  }

  const { data, error } = await supabase
    .from("manifest_entries")
    .insert({
      user_id: user.id,
      entry_date: entryDate,
      intention,
      category,
      ai_echo: null,
      keywords: [],
    })
    .select()
    .single();

  if (error) {
    return NextResponse.json({ error: error.message }, { status: 500 });
  }

  return NextResponse.json({ entry: data }, { status: 201 });
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/api/manifest/route.ts
git commit -m "feat(api): add manifest CRUD endpoint (GET + POST)"
```

---

### Task 5：星空背景组件

**Files:**
- Create: `src/components/manifest/StarfieldBackground.tsx`

- [ ] **Step 1: 写星空粒子背景组件**

```typescript
// src/components/manifest/StarfieldBackground.tsx

"use client";

import { useEffect, useRef } from "react";

interface Star {
  x: number;
  y: number;
  size: number;
  opacity: number;
  speed: number;
}

export function StarfieldBackground() {
  const canvasRef = useRef<HTMLCanvasElement>(null);

  useEffect(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;

    const ctx = canvas.getContext("2d");
    if (!ctx) return;

    const stars: Star[] = [];
    const STAR_COUNT = 120;

    function resize() {
      if (!canvas) return;
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    }

    function initStars() {
      stars.length = 0;
      for (let i = 0; i < STAR_COUNT; i++) {
        stars.push({
          x: Math.random() * (canvas?.width ?? 800),
          y: Math.random() * (canvas?.height ?? 600),
          size: Math.random() * 2 + 0.5,
          opacity: Math.random() * 0.8 + 0.2,
          speed: Math.random() * 0.005 + 0.002,
        });
      }
    }

    let animationId: number;
    let time = 0;

    function animate() {
      if (!ctx || !canvas) return;
      ctx.clearRect(0, 0, canvas.width, canvas.height);

      time += 1;

      for (const star of stars) {
        const twinkle = Math.sin(time * star.speed) * 0.3 + 0.7;
        const opacity = star.opacity * twinkle;

        // 星星主体
        ctx.beginPath();
        ctx.arc(star.x, star.y, star.size, 0, Math.PI * 2);
        ctx.fillStyle = `rgba(255, 220, 200, ${opacity})`;
        ctx.fill();

        // 较亮的星星加光晕
        if (star.size > 1.5) {
          ctx.beginPath();
          ctx.arc(star.x, star.y, star.size * 3, 0, Math.PI * 2);
          const gradient = ctx.createRadialGradient(
            star.x,
            star.y,
            0,
            star.x,
            star.y,
            star.size * 3
          );
          gradient.addColorStop(0, `rgba(255, 182, 193, ${opacity * 0.3})`);
          gradient.addColorStop(1, "rgba(255, 182, 193, 0)");
          ctx.fillStyle = gradient;
          ctx.fill();
        }
      }

      animationId = requestAnimationFrame(animate);
    }

    resize();
    initStars();
    animate();

    window.addEventListener("resize", () => {
      resize();
      initStars();
    });

    return () => {
      cancelAnimationFrame(animationId);
      window.removeEventListener("resize", resize);
    };
  }, []);

  return (
    <canvas
      ref={canvasRef}
      className="fixed inset-0 pointer-events-none"
      style={{ zIndex: 0 }}
      aria-hidden="true"
    />
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/manifest/StarfieldBackground.tsx
git commit -m "feat(manifest): add starfield canvas background with twinkle animation"
```

---

### Task 6：分类选择器与意图输入组件

**Files:**
- Create: `src/components/manifest/CategorySelector.tsx`
- Create: `src/components/manifest/IntentionInput.tsx`

- [ ] **Step 1: 写分类选择器组件**

```typescript
// src/components/manifest/CategorySelector.tsx

"use client";

import type { ManifestCategory } from "@/types/manifest";
import {
  MANIFEST_CATEGORY_LABELS,
  MANIFEST_CATEGORY_ICONS,
} from "@/types/manifest";

const CATEGORIES = Object.keys(MANIFEST_CATEGORY_LABELS) as ManifestCategory[];

interface CategorySelectorProps {
  selected: ManifestCategory | null;
  onSelect: (category: ManifestCategory) => void;
}

export function CategorySelector({ selected, onSelect }: CategorySelectorProps) {
  return (
    <div className="flex flex-wrap gap-2 justify-center">
      {CATEGORIES.map((cat) => {
        const isSelected = selected === cat;
        return (
          <button
            key={cat}
            onClick={() => onSelect(cat)}
            className={`
              px-4 py-2 rounded-full text-sm font-medium
              transition-all duration-300 ease-out
              border
              ${
                isSelected
                  ? "border-rose-gold bg-rose-gold/20 text-white shadow-[0_0_12px_rgba(255,182,193,0.3)]"
                  : "border-white/20 bg-white/5 text-white/60 hover:border-white/40 hover:text-white/80"
              }
            `}
          >
            <span className="mr-1.5">
              {MANIFEST_CATEGORY_ICONS[cat]}
            </span>
            {MANIFEST_CATEGORY_LABELS[cat]}
          </button>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 2: 写意图输入组件（文字 + 语音）**

```typescript
// src/components/manifest/IntentionInput.tsx

"use client";

import { useState, useCallback, useRef } from "react";
import { VoiceInput } from "@/components/shared/VoiceInput";

interface IntentionInputProps {
  value: string;
  onChange: (value: string) => void;
  onSubmit: () => void;
  isProcessing: boolean;
}

export function IntentionInput({
  value,
  onChange,
  onSubmit,
  isProcessing,
}: IntentionInputProps) {
  const [isVoiceActive, setIsVoiceActive] = useState(false);

  const handleVoiceResult = useCallback(
    (transcript: string) => {
      onChange(value ? `${value} ${transcript}` : transcript);
    },
    [value, onChange]
  );

  const handleKeyDown = useCallback(
    (e: React.KeyboardEvent) => {
      if (e.key === "Enter" && (e.metaKey || e.ctrlKey) && !isProcessing) {
        e.preventDefault();
        onSubmit();
      }
    },
    [onSubmit, isProcessing]
  );

  return (
    <div className="w-full max-w-lg mx-auto space-y-3">
      <textarea
        value={value}
        onChange={(e) => onChange(e.target.value)}
        onKeyDown={handleKeyDown}
        placeholder="写下你的意图、愿望、或者感谢……"
        disabled={isProcessing}
        rows={4}
        className="
          w-full bg-white/5 border border-rose-gold/30 rounded-2xl
          px-5 py-4 text-white/90 placeholder-white/30
          resize-none outline-none
          focus:border-rose-gold/60 focus:shadow-[0_0_20px_rgba(255,182,193,0.15)]
          transition-all duration-300
          disabled:opacity-50
        "
      />

      <div className="flex items-center justify-between">
        <VoiceInput
          onResult={handleVoiceResult}
          isActive={isVoiceActive}
          onToggle={() => setIsVoiceActive(!isVoiceActive)}
        />

        <button
          onClick={onSubmit}
          disabled={!value.trim() || isProcessing}
          className="
            px-6 py-2.5 rounded-full text-sm font-medium
            bg-gradient-to-r from-rose-gold/80 to-pink-400/80
            text-white shadow-[0_0_16px_rgba(255,182,193,0.3)]
            hover:shadow-[0_0_24px_rgba(255,182,193,0.5)]
            disabled:opacity-40 disabled:shadow-none
            transition-all duration-300
          "
        >
          {isProcessing ? "宇宙回声中…" : "✨ 写下意图"}
        </button>
      </div>

      <p className="text-center text-white/20 text-xs">
        Ctrl+Enter 提交 · 支持语音输入
      </p>
    </div>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add src/components/manifest/CategorySelector.tsx src/components/manifest/IntentionInput.tsx
git commit -m "feat(manifest): add category selector and intention input components"
```

---

### Task 7：显化回响气泡与关键词标签组件

**Files:**
- Create: `src/components/manifest/EchoBubble.tsx`
- Create: `src/components/manifest/KeywordTags.tsx`
- Create: `src/components/manifest/ManifestCard.tsx`

- [ ] **Step 1: 写显化回响气泡**

```typescript
// src/components/manifest/EchoBubble.tsx

"use client";

interface EchoBubbleProps {
  echo: string;
  isLoading?: boolean;
}

export function EchoBubble({ echo, isLoading }: EchoBubbleProps) {
  if (isLoading) {
    return (
      <div className="mx-auto max-w-md px-6 py-4 rounded-2xl bg-gradient-to-br from-rose-gold/10 to-pink-400/5 border border-rose-gold/20 animate-pulse">
        <div className="flex items-center gap-2 text-white/40 text-sm">
          <span className="animate-spin">✨</span>
          宇宙正在倾听……
        </div>
      </div>
    );
  }

  return (
    <div
      className="
        mx-auto max-w-md px-6 py-4 rounded-2xl
        bg-gradient-to-br from-rose-gold/10 to-pink-400/5
        border border-rose-gold/30
        shadow-[0_0_24px_rgba(255,182,193,0.15)]
        animate-fade-in
      "
    >
      <p className="text-white/90 text-base leading-relaxed italic">
        {echo}
      </p>
    </div>
  );
}
```

- [ ] **Step 2: 写关键词标签组件**

```typescript
// src/components/manifest/KeywordTags.tsx

"use client";

interface KeywordTagsProps {
  keywords: string[];
}

export function KeywordTags({ keywords }: KeywordTagsProps) {
  if (!keywords.length) return null;

  return (
    <div className="flex flex-wrap gap-2 justify-center">
      {keywords.map((keyword, i) => (
        <span
          key={`${keyword}-${i}`}
          className="
            px-3 py-1 rounded-full text-xs font-medium
            bg-rose-gold/15 border border-rose-gold/25 text-rose-gold
            animate-fade-in
          "
          style={{ animationDelay: `${i * 100}ms` }}
        >
          {keyword}
        </span>
      ))}
    </div>
  );
}
```

- [ ] **Step 3: 写单条显化记录卡片**

```typescript
// src/components/manifest/ManifestCard.tsx

"use client";

import type { ManifestEntry } from "@/types/manifest";
import {
  MANIFEST_CATEGORY_LABELS,
  MANIFEST_CATEGORY_ICONS,
} from "@/types/manifest";
import { KeywordTags } from "./KeywordTags";

interface ManifestCardProps {
  entry: ManifestEntry;
}

export function ManifestCard({ entry }: ManifestCardProps) {
  const time = new Date(entry.createdAt).toLocaleTimeString("zh-CN", {
    hour: "2-digit",
    minute: "2-digit",
  });

  return (
    <div className="bg-white/5 border border-white/10 rounded-2xl p-5 space-y-3">
      {/* 头部：分类 + 时间 */}
      <div className="flex items-center justify-between text-sm">
        <span className="text-rose-gold/80">
          {MANIFEST_CATEGORY_ICONS[entry.category]}{" "}
          {MANIFEST_CATEGORY_LABELS[entry.category]}
        </span>
        <span className="text-white/30">{time}</span>
      </div>

      {/* 意图原文 */}
      <p className="text-white/80 text-sm leading-relaxed">
        {entry.intention}
      </p>

      {/* AI 回响 */}
      {entry.aiEcho && (
        <div className="pl-3 border-l-2 border-rose-gold/30">
          <p className="text-white/60 text-sm italic">{entry.aiEcho}</p>
        </div>
      )}

      {/* 关键词 */}
      {entry.keywords.length > 0 && <KeywordTags keywords={entry.keywords} />}
    </div>
  );
}
```

- [ ] **Step 4: Commit**

```bash
git add src/components/manifest/EchoBubble.tsx src/components/manifest/KeywordTags.tsx src/components/manifest/ManifestCard.tsx
git commit -m "feat(manifest): add echo bubble, keyword tags, and manifest card components"
```

---

### Task 8：显化日记主页面

**Files:**
- Create: `src/app/manifest/page.tsx`

- [ ] **Step 1: 写显化日记主页面**

```typescript
// src/app/manifest/page.tsx

"use client";

import { useState, useCallback } from "react";
import { StarfieldBackground } from "@/components/manifest/StarfieldBackground";
import { CategorySelector } from "@/components/manifest/CategorySelector";
import { IntentionInput } from "@/components/manifest/IntentionInput";
import { EchoBubble } from "@/components/manifest/EchoBubble";
import { KeywordTags } from "@/components/manifest/KeywordTags";
import { ManifestCard } from "@/components/manifest/ManifestCard";
import type { ManifestCategory, ManifestEntry } from "@/types/manifest";

export default function ManifestPage() {
  const [intention, setIntention] = useState("");
  const [category, setCategory] = useState<ManifestCategory | null>(null);
  const [isProcessing, setIsProcessing] = useState(false);
  const [echo, setEcho] = useState<string | null>(null);
  const [keywords, setKeywords] = useState<string[]>([]);
  const [insight, setInsight] = useState<string | null>(null);
  const [todayEntries, setTodayEntries] = useState<ManifestEntry[]>([]);
  const [showHistory, setShowHistory] = useState(false);

  const handleSubmit = useCallback(async () => {
    if (!intention.trim() || !category || isProcessing) return;

    setIsProcessing(true);
    setEcho(null);
    setKeywords([]);
    setInsight(null);

    try {
      // 1. 创建 manifest entry
      const createRes = await fetch("/api/manifest", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ intention, category }),
      });
      const { entry } = await createRes.json();
      const entryId = entry.id;

      // 2. 获取显化回响
      const echoRes = await fetch("/api/ai/echo", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ entryId, intention }),
      });
      const echoData = await echoRes.json();
      setEcho(echoData.echo);

      // 3. 获取关键词分析
      const analyzeRes = await fetch("/api/ai/analyze", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ entryId, intention }),
      });
      const analyzeData = await analyzeRes.json();
      setKeywords(analyzeData.keywords ?? []);
      setInsight(analyzeData.insight ?? null);

      // 4. 刷新今日记录
      const today = new Date().toISOString().split("T")[0];
      const listRes = await fetch(`/api/manifest?date=${today}`);
      const listData = await listRes.json();
      setTodayEntries(listData.entries ?? []);

      // 5. 清空输入
      setIntention("");
      setCategory(null);
    } catch (err) {
      console.error("Manifest submission failed:", err);
      setEcho("星尘暂时迷路了……请再试一次 ✨");
    } finally {
      setIsProcessing(false);
    }
  }, [intention, category, isProcessing]);

  return (
    <div className="min-h-screen bg-gradient-to-b from-[#0f0a2e] via-[#1a1145] to-[#0d0820] text-white relative overflow-hidden">
      <StarfieldBackground />

      <div className="relative z-10 max-w-2xl mx-auto px-4 py-8 space-y-8">
        {/* 页面标题 */}
        <header className="text-center space-y-2">
          <h1 className="text-2xl font-light tracking-wider text-white/90">
            ✨ 显化仪式
          </h1>
          <p className="text-sm text-white/30">
            写下你的意图，宇宙会回应
          </p>
        </header>

        {/* 分类选择 */}
        <section>
          <CategorySelector selected={category} onSelect={setCategory} />
        </section>

        {/* 意图输入 */}
        <section>
          <IntentionInput
            value={intention}
            onChange={setIntention}
            onSubmit={handleSubmit}
            isProcessing={isProcessing}
          />
        </section>

        {/* AI 回响 */}
        {(echo || isProcessing) && (
          <section className="space-y-4">
            <EchoBubble echo={echo ?? ""} isLoading={isProcessing && !echo} />
            {keywords.length > 0 && <KeywordTags keywords={keywords} />}
            {insight && (
              <p className="text-center text-white/40 text-sm italic">
                💡 {insight}
              </p>
            )}
          </section>
        )}

        {/* 今日显化历史 */}
        {todayEntries.length > 0 && (
          <section className="space-y-3">
            <button
              onClick={() => setShowHistory(!showHistory)}
              className="text-sm text-white/30 hover:text-white/50 transition-colors"
            >
              {showHistory ? "收起" : "查看"}今日显化（{todayEntries.length}）
            </button>

            {showHistory && (
              <div className="space-y-3">
                {todayEntries.map((entry) => (
                  <ManifestCard key={entry.id} entry={entry} />
                ))}
              </div>
            )}
          </section>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/manifest/page.tsx
git commit -m "feat(manifest): add manifest diary page with starfield, echo, and analysis"
```

---

### Task 9：显化页面 CSS 变量与 Tailwind 配置

**Files:**
- Modify: `tailwind.config.ts`

- [ ] **Step 1: 在 tailwind.config.ts 中添加显化主题色**

在 `theme.extend.colors` 中添加：

```typescript
colors: {
  // ... 已有的夜晚主题色
  "rose-gold": {
    DEFAULT: "#FFB6C1",
    50: "#FFF0F3",
    100: "#FFE0E8",
    200: "#FFC1D5",
    300: "#FFB6C1", // DEFAULT
    400: "#FF9EB0",
    500: "#FF7A95",
    600: "#E5567A",
    700: "#CC335F",
    800: "#B31A45",
    900: "#991130",
  },
},
```

在 `theme.extend.animation` 中添加：

```typescript
animation: {
  // ... 已有的动画
  "fade-in": "fadeIn 0.6s ease-out forwards",
},
keyframes: {
  fadeIn: {
    "0%": { opacity: "0", transform: "translateY(8px)" },
    "100%": { opacity: "1", transform: "translateY(0)" },
  },
},
```

- [ ] **Step 2: Commit**

```bash
git add tailwind.config.ts
git commit -m "feat(theme): add rose-gold color scale and fade-in animation for manifest theme"
```

---

### Task 10：端到端测试——显化流程手动验证

**Files:** 无新文件

- [ ] **Step 1: 启动开发服务器**

Run: `npm run dev`

- [ ] **Step 2: 测试显化日记完整流程**

1. 打开 `/manifest` 页面
2. 确认星空背景渲染正常、星星闪烁
3. 选择一个分类（如"自我 🌱"），确认按钮高亮
4. 输入意图文字："我已是一个平静有秩序的人"
5. 点击「✨ 写下意图」
6. 确认 AI 回响出现（≤50 字、温柔笃定、无分析/建议）
7. 确认关键词标签出现（3-5 个）
8. 确认点睛话出现（≤30 字）
9. 点击「查看今日显化」，确认历史卡片出现

- [ ] **Step 3: 测试语音输入**

1. 点击 🎤 按钮开始录音
2. 说一段意图："我希望我能找到一份让我真正有热情的工作"
3. 再次点击 🎤 停止
4. 确认文字出现在输入框
5. 提交，确认 AI 回响正常

- [ ] **Step 4: 测试错误处理**

1. 不选分类直接提交 → 确认按钮是 disabled 的
2. 不输入文字直接提交 → 确认按钮是 disabled 的
3. 清空意图提交 → 确认无法提交

- [ ] **Step 5: 测试移动端**

1. 在手机浏览器打开 `/manifest`
2. 确认布局响应式正常（单列、按钮可点击、语音可用）

- [ ] **Step 6: Commit 测试通过记录**

```bash
git commit --allow-empty -m "test(manifest): e2e manifest flow verified - echo, analysis, voice, responsive"
```

---

## M4 完成标志

- ✅ `/manifest` 页面可用，星空视觉正常
- ✅ 写意图 + 选分类 → AI 回响（≤50 字）正常返回
- ✅ AI 关键词提取 + 点睛话正常返回
- ✅ 语音输入可用
- ✅ 今日历史记录可展开查看
- ✅ 移动端响应式可用
